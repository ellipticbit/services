<Grid x:Class="EllipticBit.Services.Dialog.WPF.DialogHost"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	  xmlns:sf="http://schemas.syncfusion.com/wpf"
	  mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="900">

	<Grid.RowDefinitions >
		<RowDefinition Height="*" />
		<RowDefinition Height="1.5*" />
		<RowDefinition Height="*" />
	</Grid.RowDefinitions>

	<Grid.ColumnDefinitions >
		<ColumnDefinition Width="*" />
		<ColumnDefinition Width="2*" />
		<ColumnDefinition Width="*" />
	</Grid.ColumnDefinitions>

	<Border Background="{DynamicResource PrimaryForegroundDisabled}" Grid.RowSpan="3" Grid.ColumnSpan="3" />

	<Border Background="{DynamicResource PopupBackground}" Grid.Row="1" Grid.ColumnSpan="3" />
	
	<ContentPresenter x:Name="MessageContent" Grid.Row="1" Grid.Column="1" Margin="20" />
	
</Grid>
