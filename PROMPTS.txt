EllipticBit Services - AI / LLM Contribution Prompt Log
========================================================

Per the contribution guidelines, every contribution that was generated (in whole
or in part) with the assistance of a Large Language Model (LLM) or other AI tool
MUST record the prompt(s) used to produce it in this file, as part of the same
pull request. Append a new entry below for each AI-assisted contribution.

--------------------------------------------------------------------------------

/plan We need to make two related changes to every project file in this solution. We also need to apply this plan to the projects in Abstractions.sln file in this same git repo.

First, we need to update each project file to include all the required properties for a Nuget package deployed to nuget.org. The icon file is here: <file>. Additionally in order to comply with nuget.org standards, we need to generate anappropriate markdown readme for this project and place the text into the README.md file. Include a brief description of the project and the standard information typically included in a README for an open source project, how to add the library to a project, an appropriate number of example of how to integrate (typically via DI) and use the library, and contribution guidelines that include a note saying that any LLM based contributions must include the Prompt used to generate the contribution in the PROMPTS.txt file.

Second, we need to ensure that each project assembly that we build is signed using Azure Artifact Signing. Additionally we want to sign each generated nupkg prior to pushing to nuget.org. To achieve this we want to add the project property to have the project packaged as part of the build process. As a result we would like to implement the assembly and nuget signing as custom build targets in the project files, one for the assembly build and another for the nupkg signing. The signing parameters for Azure Artifact signing will be available as Environment variables, you can select the names, but they must be the same across each project.